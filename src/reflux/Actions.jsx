var Reflux = require('reflux');

/**
 * Actions for communication with the DataStore
 * @namespace ChartActions
 */
var ChartActions = Reflux.createActions([
    'getData'
]);

module.exports = ChartActions;